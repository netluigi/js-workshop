/*
Exercício: Crie uma função que receba uma string como input, a inverta essa string
e substitua todas as vogais pela próxima vogal do alfabeto (a -> e, e -> i, i -> o, o -> u, u -> a ).

Instruções:
Criar uma função chamada reverseAndReplaceVowels que aceite um argumento do tipo string chamada input.
Inverta a string de entrada.
Substitua todas as vogais na string invertida pela próxima vogal do alfabeto.
Retorna a string modificada.
Teste sua função com algumas strings de amostra para garantir que funcione conforme o esperado.

Testar a função com algumas strings
console.log(reverseAndReplaceVowels("hello")); // Resultado esperado: "ullih"
console.log(reverseAndReplaceVowels("javascript")); // Resultado esperado: "tpircsivijo"
*/

function reverseAndReplaceVowels(input) {
    // Passo 1: Inverte a string de entrada

    // Passo 2: Substitua todas as vogais na string invertida pela próxima vogal no alfabeto

    // Passo 3: Retornar a string modificada
}